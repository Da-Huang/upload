$(document).ready(function() {
    /*
  $('.inputfile-base').change(function(e) {
    var filenames = '';
    console.log(this);
    console.log(e);
    var spantext = $(this).parent().find('span');
    if (this.files && this.files.length > 0) {
      spantext.text(this.files.length + ' files choosen');
    } else {
      spantext.text('Choose a file...');
    }
  });
  */
});
